extends ../layout

block content 
  h1 Genre ID #{title}
  h2= genre.name 
  p= genre.description 

  if (genreGames)
    p #{genre.name} Games:
    for game in genreGames 
      a(href=game.url)
        button= game.name 